name: star_featurecounts-featurecounts
channels:
  - bioconda
  - conda-forge
dependencies:
  # subread v2.1.1 provides the featureCounts binary. Exact match to
  # Plasmidsaurus's methods.txt ("subread v2.1.1").
  - subread=2.1.1
